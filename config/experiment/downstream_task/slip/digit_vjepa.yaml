# @package _global_
defaults:
  - /paths: ~
  - /wandb: ~
  - /data: digit_force
  - /task: t2_slip_detection 
  - _self_

ssl_name: vjepa
sensor: digit
ckpt_path: ~

data:
  dataset:
    config:
      out_format: video
      num_frames: 4
      frame_stride: 2

task:
  model_encoder:
    in_chans: 3
    num_frames: 4
